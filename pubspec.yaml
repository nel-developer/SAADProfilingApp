name: da_project_1
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.1.0
  firebase_core: ^3.0.0
  firebase_auth: ^5.0.0
  cloud_firestore: ^5.0.0
  shared_preferences: ^2.0.15
  image_picker: ^1.0.7  
  fl_chart: ^0.68.0
  signature: ^5.4.0
  hive: ^2.2.0
  hive_flutter: ^1.1.0
  connectivity_plus: ^5.0.0
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/images/2.png
    - assets/images/3.png
    - assets/philippines_complete.json